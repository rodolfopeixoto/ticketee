<% title(@project.name, 'Projects') %>

<h1><%= @project.name %></h1>
<p><%= @project.description %></p>

<%= link_to 'Edit Project', edit_project_path(@project) %>
<%= link_to 'Delete Project', project_path(@project),
                             method: :delete,
                             data: { confirm: 'Area you sure you want to delete this project?' } %>